# Python dependencies for the Vercel serverless API (api/index.py).
#
# Vercel installs these from the project root. Keep in step with
# backend/requirements.txt, which is what local development and the Docker
# image use.
fastapi>=0.115.0
pydantic>=2.7.0
httpx>=0.27.0
reportlab>=4.0.0
python-dotenv>=1.0.0
groq>=0.11.0
