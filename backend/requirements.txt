# Runtime dependencies.
#
# weasyprint and google-generativeai were removed: neither is imported
# anywhere. weasyprint in particular needs cairo/pango system libraries and is
# a common cause of failed builds on slim Python hosts. PDFs use reportlab and
# the LLM calls go through groq.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
httpx>=0.27.0
reportlab>=4.0.0
python-dotenv>=1.0.0
groq>=0.11.0

# Postgres driver. Only loaded when DATABASE_URL (or POSTGRES_URL) is set,
# which is how durable storage is switched on; see app/core/db.py.
psycopg[binary]>=3.2
